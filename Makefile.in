# Makefile.in generated by unsermake
####################################

top_builddir = ..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit apidox apidox-am-no apidox-am-toplevel-no apidox-am-toplevel-yes apidox-am-yes install-apidox install-data-local uninstall-apidox uninstall-local

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) force-reedit

apidox:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) apidox

apidox-am-no:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) apidox-am-no

apidox-am-toplevel-no:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) apidox-am-toplevel-no

apidox-am-toplevel-yes:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) apidox-am-toplevel-yes

apidox-am-yes:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) apidox-am-yes

install-apidox:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install-apidox

install-data-local:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install-data-local

uninstall-apidox:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) uninstall-apidox

uninstall-local:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) uninstall-local

$(top_builddir)/superkaramba/Makefile: $(top_builddir)/config.status $(top_srcdir)/superkaramba/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status superkaramba/Makefile

all: $(top_builddir)/superkaramba/Makefile
final: 
	@echo no programs available to recreate as final. Call in subdirs

.PHONY: all final
