# Makefile.in generated by unsermake
####################################

top_builddir = ../..
top_srcdir = ../..
srcdir = .
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	$(UNSERMAKE) force-reedit

$(top_builddir)/superkaramba/mimetypes/Makefile: $(top_builddir)/config.status $(top_srcdir)/superkaramba/mimetypes/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status superkaramba/mimetypes/Makefile

all: $(top_builddir)/superkaramba/mimetypes/Makefile
final: 
	@echo no programs available to recreate as final. Call in subdirs

.PHONY: all final
